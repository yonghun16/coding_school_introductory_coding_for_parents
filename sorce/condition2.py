if(블럭이 하단에 맞닿았는가?):
    if(하단 블럭이 다 매꿔졌는가?):
        블럭을 삭제하는 함수()
        점수를 올리는 함수()
        위 블럭을 한칸 내리는 함수()
        새로운 블럭을 생성하는 함수()
else:
    if(맨위칸에 블럭이 상단에 맞닿았는가?):
        게임 끝을 알리는 함수()



